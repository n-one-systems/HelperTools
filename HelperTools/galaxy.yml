#SPDX-License-Identifier: MIT-0
### REQUIRED
# The namespace of the collection. This can be a company/brand/organization or product namespace under which all
# content lives. May only contain alphanumeric lowercase characters and underscores. Namespaces cannot start with
# underscores or numbers and cannot contain consecutive underscores
namespace: nsys

# The name of the collection. Has the same character restrictions as 'namespace'
name: HelperTools

# The version of the collection. Must be compatible with semantic versioning
version: 0.1.0

# The path to the Markdown (.md) readme file. This path is relative to the root of the collection
readme: README.md

authors:
- ai-working-group


### OPTIONAL but strongly recommended
# A short summary description of the collection
description: A collection of helper tools

# Either a single license or a list of licenses for content inside of a collection. Ansible Galaxy currently only
# accepts L(SPDX,https://spdx.org/licenses/) licenses. This key is mutually exclusive with 'license_file'
license:
- GPL-3.0-or-later

# The path to the license file for the collection. This path is relative to the root of the collection. This key is
# mutually exclusive with 'license'
license_file: ''

# A list of tags you want to associate with the collection for indexing/searching. A tag name has the same character
# requirements as 'namespace' and 'name'
tags:
tags:
  - linux
  - networking
  - cloud
  - utilities
  - ipv4

dependencies: {}

# The URL of the originating SCM repository
repository: https://github.com/n-one-systems/HelperTools
# The URL to any online docs
documentation: https://github.com/n-one-systems/HelperTools

# The URL to the homepage of the collection/project
homepage: https://github.com/n-one-systems/HelperTools

# The URL to the collection issue tracker
issues: https://github.com/n-one-systems/HelperTools/issues

build_ignore: []

